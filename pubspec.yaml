name: typed_firestore
description: Typed firebase.storage for flutter.

environment:
  sdk: '>=2.0.0-dev.54 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  built_value: '^6.1.3'
  cloud_firestore: '0.8.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: '^3.0.0'
  built_value_generator: '^6.1.3'
  build_runner: '^0.10.2'

